package doubleCheckedLocking;


/**
 * Double checking locking with Signleton
 *u sed to reduce the overhead of acquiring a lock by testing the locking criterion (the "lock hint")
 * before acquiring the lock. Locking occurs only if the locking criterion check indicates that locking is required.
 *
 *
 * It is typically used to reduce locking overhead when implementing "lazy initialization"
 *
 *
 *
 *
 * author: petar
 */
public class App {

    public static void main(String[] arags) {



    }
        }